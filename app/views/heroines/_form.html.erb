<%= form_with(model: heroine, local: true) do |form| %>
  <% if heroine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heroine.errors.count, "error") %> prohibited this heroine from being saved:</h2>

      <ul>
      <% heroine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
