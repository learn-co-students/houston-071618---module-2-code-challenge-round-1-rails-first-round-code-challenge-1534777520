<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine %>
    </li>
    <br>
  <% end %>
</ul>


<%= form_tag(heroines_path, method: :get) do %>
  <%= text_field_tag :name %>
  <%= submit_tag 'Search' %>
<% end %>